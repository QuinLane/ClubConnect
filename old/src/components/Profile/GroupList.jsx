/**
 * Displays a list of groups the user has joined
 *
 * Props:
 * -groups: An array of groups the user has joined (e.g., [{ name: "Group Name", school: "School Name" }])
 *
 * Functionality:
 * -Renders a section with the heading "Groups Joined"
 * -Maps over the groups array to display each group’s name and school
 *
 * State:
 * -None
 */
