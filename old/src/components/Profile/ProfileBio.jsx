/**
 * Displays the user’s bio
 *
 * Props:
 * -bio: The user’s bio text
 *
 * Functionality:
 * -Renders a section with the heading "About"
 * -Displays the user’s bio text (or a placeholder if empty)
 *
 * State:
 * -None
 */
