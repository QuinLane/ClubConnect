/**
 *  Provides nagivation control to browse through multiple pages of search results
 *
 *  Props:
 *  -currentPage: The current page number (e.g., 2)
 *  -totalPages: the total number of pages (e.g., 11)
 *  -onPageChange: a callback to handle page changes (e.g., when the user clicks 'Next')
 *
 *
 *  Functionaliy:
 *  -Renders "Previous" and "Next" buttons
 *  -Displays page numbers (e.g., 1,2,3,4....11) with the current page highlighted
 *  -Calls onPageChange with the new page number when the user clicks a page number or navigation button
 *
 *  State:
 *  -None (would be managed by parent)
 *
 *
 */
